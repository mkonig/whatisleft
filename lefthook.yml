commit-msg:
  commands:
    "check commit message":
      run: cog verify --file {1}

pre-commit:
  parallel: true
  commands:
    bats:
      run: bats test
    shellcheck:
      glob: "*.sh"
      run: shellcheck {staged_files}
